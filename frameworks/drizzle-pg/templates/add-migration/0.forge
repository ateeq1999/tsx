{# forge:tier feature #}
-- Migration: {{ name }}
-- Generated by tsx run add:migration
-- Run: npx drizzle-kit push   (apply immediately)
--   or npx drizzle-kit generate  (generate SQL only)

-- Write your migration SQL below.
-- Example:
-- CREATE TABLE IF NOT EXISTS "{{ name | snake_case }}" (
--   "id" serial PRIMARY KEY NOT NULL,
--   "created_at" timestamp DEFAULT now() NOT NULL
-- );
