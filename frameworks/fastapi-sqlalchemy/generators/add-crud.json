{
  "id": "add-crud",
  "command": "add:crud",
  "description": "Add a CRUD service layer (repository pattern) for a SQLAlchemy model",
  "token_estimate": 35,
  "output_paths": [
    "app/crud/{{name | snake_case}}.py"
  ],
  "next_steps": [
    "Inject the CRUD service into your router via dependency injection",
    "Add custom query methods for domain-specific operations"
  ],
  "schema": {
    "type": "object",
    "required": ["name"],
    "properties": {
      "name": { "type": "string", "description": "Model name the CRUD operates on (e.g. 'User')" },
      "async_mode": { "type": "boolean", "default": true, "description": "Use async SQLAlchemy session (asyncpg)" },
      "soft_delete": { "type": "boolean", "default": false, "description": "Filter deleted_at in all queries" }
    }
  }
}
