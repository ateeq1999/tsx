/target

# Runtime data — never committed
data/
.runtime/

# Local dev secrets — never commit (contains real DB credentials)
Secrets.dev.toml

# Vercel project linkage (local only)
.vercel/

# Local env files
.env
.env.*
*.local

# Misc
.DS_Store
count.txt
todos.json
node_modules
