/target
# Runtime data (tarballs, registry DB) — never committed
data/
.runtime/
# Shuttle local dev secrets — never commit (contains real DB credentials)
Secrets.dev.toml
node_modules
.DS_Store
dist
dist-ssr
*.local
count.txt
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json
