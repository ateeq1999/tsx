{
  "id": "add-auth-guard",
  "command": "add:auth-guard",
  "description": "Wrap a route or server function with a Better Auth session guard that redirects unauthenticated users",
  "token_estimate": 18,
  "output_paths": [
    "middleware/{{name}}-guard.ts"
  ],
  "next_steps": [
    "Import and apply the guard in your route file",
    "Set `BETTER_AUTH_SECRET` in your `.env` if not already done"
  ],
  "schema": {
    "type": "object",
    "required": ["name"],
    "properties": {
      "name": {
        "type": "string",
        "description": "Guard name / route context (e.g. 'dashboard', 'admin', 'api')"
      },
      "redirect_to": {
        "type": "string",
        "default": "/login",
        "description": "Path to redirect unauthenticated users to"
      },
      "role": {
        "type": "string",
        "description": "Optional required role (e.g. 'admin', 'moderator')"
      }
    }
  }
}
