{
  "id": "add-router",
  "command": "add:router",
  "description": "Add a FastAPI APIRouter with CRUD endpoints",
  "token_estimate": 40,
  "output_paths": [
    "app/routers/{{name | snake_case}}.py"
  ],
  "next_steps": [
    "Register the router in app/main.py with `app.include_router(...)`",
    "Add authentication dependencies as needed"
  ],
  "schema": {
    "type": "object",
    "required": ["name"],
    "properties": {
      "name": { "type": "string", "description": "Resource name (e.g. 'User', 'Product')" },
      "prefix": { "type": "string", "description": "URL prefix (e.g. '/users')" },
      "tag": { "type": "string", "description": "OpenAPI tag for grouping (defaults to name)" },
      "auth_required": { "type": "boolean", "default": false, "description": "Add OAuth2/JWT auth dependency to all endpoints" }
    }
  }
}
