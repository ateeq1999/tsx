# ── Production secrets ────────────────────────────────────────────────────────
# Set real values with the Shuttle CLI (never commit real credentials):
#
#   shuttle secret set DATABASE_URL         "postgresql://..."
#   shuttle secret set TSX_REGISTRY_API_KEY "your-strong-secret"
#   shuttle secret set DATA_DIR             "./data"
#
# Alternatively, fill in the values below — Shuttle encrypts them server-side.
# Keep this file in .gitignore if the repo is public.

DATABASE_URL         = ""
TSX_REGISTRY_API_KEY = ""
DATA_DIR             = "./data"
