# Environment variable template for tsx-registry.
# Set real values in the Railway dashboard under Variables — never commit credentials.

DATABASE_URL         = ""
TSX_REGISTRY_API_KEY = ""
DATA_DIR             = "./data"
